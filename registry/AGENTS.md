# Registry

Scope: blocks and consumer guidance. HITL required. One view may yield multiple experimental block slices over time. `USAGE.md` and `registryDependencies` must match imports. Skill: `example-promote-block`. Do not mark stable in the creation PR. Do not commit generated `public/r`. After HITL ack, **propose** a catalog-memory fact; **Documentation (`ds-docs`)** writes a short record to `shared/` after ack ([docs/AGENT-MEMORY.md](../docs/AGENT-MEMORY.md)); do not write memory in the same turn as promotion.
