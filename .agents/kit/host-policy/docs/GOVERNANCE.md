# Governance (host policy stub)

Separate lifecycle PR required for stable promotion.

Stable status cannot be bundled into initial creation.

Host-specific HITL for tokens, primitives, blocks, and release — replace this stub on real hosts.
