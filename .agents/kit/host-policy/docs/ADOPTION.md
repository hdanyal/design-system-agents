# Adoption (host policy stub)

Ship this stub on greenfield hosts; replace with your registry contract.

- Pin immutable `/r/vX.Y.Z` releases.
- Keep a host lockfile (`host.lock.json` or `{packId}.lock.json`).
- Never point production apps at `/r/dev` or `/latest`.
