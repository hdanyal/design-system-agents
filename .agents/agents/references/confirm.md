# Confirm before action

Stop and ask before: first protected write in a session; first write to `.agents/program/`; new primitive/block; pack `id` or `bootstrapStatus`; Figma writes; launching another specialist; kit install/upgrade; creating `paths.prototypes`; installing a preview surface.

v1: refuse importing another design system’s files or memory (do not treat as confirm).

Read-only scan/explain needs no confirm.

Prompt: owner agent, one-sentence action, paths or diff, open blocking gaps, proposed next agent.

Options: Approve / Approve with edits / Pass to a different agent / Stop.

Cursor: `AskQuestion`. Claude Code / Codex: stop and wait for an explicit yes. Auto-edit is not kit confirm.

Kit confirm never satisfies GOVERNANCE HITL. Agent review is evidence only.
