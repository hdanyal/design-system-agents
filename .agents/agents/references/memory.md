# Memory retrieval

Host-owned reviewed notes under `.agents/memory/`. Not copied by kit pack/install/upgrade records.

**Must read:** [docs/AGENT-MEMORY.md](../../../docs/AGENT-MEMORY.md) (policy).

**Do not** read every file under `.agents/memory/` in one turn. Session start (Cursor) prints shared title counts only — never record bodies.

**When to open `shared/`:** this turn names a catalog entity and you need its `do-not-clone` / decision fact. Match by entity slug or title.

**When to open own namespace (`ds-bugbot`, `ds-security`, `ds-a11y`):** recording or acking evidence, or the user asked about a prior finding.

Inventory, Storybook, and USAGE remain source for what exists. Memory stores decisions inventory cannot express.
