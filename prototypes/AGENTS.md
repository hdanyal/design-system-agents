# Prototypes

Scope: sandbox only. Any human-named view is allowed (no subject-matter allowlist).

## Required

- `USAGE.md` (inventory + composed-from + local layout + living **Harvest** flags)
- Storybook CSF under Prototypes
- Live companion after every material write — pack `preview` (`commands.preview`). Print surface, command, story URL, path, `shown: yes|no`. Chat JSX and Cursor Canvas are not the gallery.

## Skills

Follow this pack's prototype then compose skills under `.agents/skills/` when present. Read generated branding `reference.md` under `.agents/skills/` when present (CSS variables only).

## Harvest

Flags: `reuse` | `enhance-existing` | `extract-new primitive` | `extract-new block` | `keep local`. Prefer that order. Batch into one Architect hop using `.agents/agents/references/harvest-map.md`. If USAGE grows large, use `HARVEST.md` and link it from USAGE.

## Forbidden

Do not publish, promote, mark stable, or commit `public/r` from here. Do not restyle pack `paths.ui` or invent twin public APIs.
