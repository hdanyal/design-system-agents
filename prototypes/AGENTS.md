# Prototypes

Scope: sandbox only. Any human-named view is allowed (no subject-matter allowlist).

## Required

- `USAGE.md` (inventory + composed-from + local layout + living **Harvest** flags)
- Storybook CSF under Prototypes
- Live companion after every material write — pack `preview` (Carina: `pnpm storybook`). Print surface, command, story URL, path, `shown: yes|no`. Chat JSX and Cursor Canvas are not the gallery.

## Skills

`carina-prototype` then `carina-compose`. Read generated `.agents/skills/carina-branding/reference.md` (CSS variables only).

## Harvest

Flags: `reuse` | `enhance-existing` | `extract-new primitive` | `extract-new block` | `keep local`. Prefer that order. Batch into one Architect hop using `.agents/inventory/proposals/_template-harvest-map.md`. If USAGE grows large, use `HARVEST.md` and link it from USAGE.

## Forbidden

Do not publish, promote, mark stable, or commit `public/r` from here. Do not restyle `components/ui` or invent twin public APIs.
