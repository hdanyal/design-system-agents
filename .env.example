# Registry authentication. Never commit real values.
# Local and CI inject this at runtime only.
EXAMPLE_REGISTRY_TOKEN=

# Optional override for served registry origin in consumer fixtures.
EXAMPLE_REGISTRY_ORIGIN=http://localhost:3000
