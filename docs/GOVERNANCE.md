# Governance (kit)

Kit changes: confirm before writes, one owning specialist per request on a **host** checkout.

Host-specific HITL (tokens, primitives, blocks, release) is defined on each host after install — see stub files under `.agents/kit/host-policy/` copied on first install.
