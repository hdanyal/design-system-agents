# Skills

GENERATED from `.agents/skills/manifest.json`. Canonical instructions live in each `SKILL.md`.

| Skill | Form | Invocation | Boundary |
| --- | --- | --- | --- |
| `example-onboard` | thin | contextual | Pointer to pack bootstrap (ds-release) then Manager for the board; not a second cold start |
| `example-branding` | thin | auto-select | Generated branding identity (Overview, CSS vars, Do's and Don'ts); no hex in JSX; no token policy duplication |
| `example-compose` | full | auto-select | Inventory, harvest-while-building, enhance-existing over twins, composition map, extraction routing |
| `example-a11y` | full | auto-select | Automated/manual accessibility workflow and evidence |
| `example-contribute` | full | auto-select | Execute CONTRIBUTING process without restating it |
| `example-prototype` | thin | contextual | Any human-named view in prototypes with live Storybook present and harvest flags; never promote |
| `example-extend-ui` | full | contextual | Propose/build new or enhance named host primitives; rewire sandboxes after extract |
| `example-promote-block` | full | contextual | Experimental multi-slice block promotion with HITL; propose memory after ack, not same turn |
| `example-update-design-language` | full | contextual | Preset candidate or canonical token change workflow |
| `example-update-shadcn` | full | contextual | Pinned upstream refresh and patch-ledger rebase |
| `example-figma` | full | contextual | Bridge official Figma workflows to Example policy |
| `example-shadcn-mcp` | thin | contextual | Browse/view/install routing and registry-origin checks |
| `example-stories` | full | contextual | Story coverage, interaction tests, tags, and docs |
| `example-verify` | thin | contextual | Select and report deterministic checks |
| `example-consume` | full | contextual | Immutable install/update/drift flow under ADOPTION |
| `example-release` | full | explicit-only | Version PR, immutable release, attestations, and HITL |
| `example-lifecycle` | full | explicit-only | Stable/deprecate/replace/remove entity lifecycle |
| `example-incident` | full | explicit-only | INCIDENTS rollback/revocation workflow |
| `example-dependency-review` | thin | explicit-only | SECURITY intake evidence and approval routing |
| `example-agent-tooling` | full | explicit-only | Skills, rules, hooks, MCP, adapters, and vendor locks |
