# Carina primitives

Scope: reusable primitives only. Prefer enhancing a named existing API over inventing a twin. New files require `RATIONALE.md`, stories, a11y `error`, and `meta.json`. Wrap stock; do not copy it. Skill: `carina-extend-ui`. After extract, rewire consuming prototypes to catalog imports and present the live Storybook companion.
