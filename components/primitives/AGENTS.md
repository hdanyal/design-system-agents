# Primitives

Scope: reusable host primitives under pack `paths.primitives`. Prefer enhancing a named existing API over inventing a twin. New files require `RATIONALE.md`, stories, a11y `error`, and `meta.json`. Wrap stock; do not copy it. Follow this pack's extend-ui skill under `.agents/skills/` when present. After extract, rewire consuming prototypes to catalog imports and present the live Storybook companion.
