# Code Connect gap

No Figma file is linked on this pack (`figmaFileKey` is unset). Manager tasks a file on the program board when Code Connect or design exploration is needed. Do not invent a file URL.

Add `heading-group.figma.ts` in the same lifecycle as the Figma component when a kit exists in the pack file.
